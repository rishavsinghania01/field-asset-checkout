[tool.ruff]
line-length = 110
target-version = "py312"
extend-exclude = ["assets/migrations"]

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]
ignore = ["B904"]

[tool.ruff.lint.isort]
known-first-party = ["assets", "config"]
